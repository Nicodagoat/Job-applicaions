# ============================================================
# Job Application Platform — Python Dependencies
# ============================================================

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# HTTP client (scrapers, API calls, job validation)
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0

# Browser automation (form auto-filling)
playwright==1.49.0

# AI — Groq client (free API, LLaMA 3.1 70B)
groq==0.11.0
# Ollama: no package needed — uses plain HTTP requests
# OpenAI (optional paid fallback): pip install openai

# Data validation
pydantic==2.10.3

# YAML config
pyyaml==6.0.2

# Environment variables
python-dotenv==1.0.1

# File upload support
python-multipart==0.0.20

# Scheduling (optional, for automated runs)
apscheduler==3.10.4

# Testing
pytest==8.3.4
httpx==0.28.1
